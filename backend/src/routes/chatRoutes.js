// Backward compatibility file - imports from the new modular route structure
// This maintains the same external API while using the refactored internal structure

module.exports = require('./index');